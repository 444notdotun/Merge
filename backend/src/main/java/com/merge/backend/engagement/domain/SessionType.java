package com.merge.backend.engagement.domain;

public enum SessionType {
    /** Student's primary work is completing drills for the current concept. */
    DRILL,
    /** Build is unlocked for the student's current stage — focus shifts to the build. */
    BUILD
}
